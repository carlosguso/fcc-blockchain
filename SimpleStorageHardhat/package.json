{
  "name": "hardhat-project",
  "scripts": {
    "test:ganache": "npx hardhat --network ganache test",
    "test:localhost": "npx hardhat --network localhost test",
    "deploy:ganache": "npx hardhat run --network ganache scripts/deploy.ts",
    "deploy:localhost": "npx hardhat run --network localhost scripts/deploy.ts",
    "deploy:rinkeby": "npx hardhat run --network rinkeby scripts/deploy.ts",
    "verify:rinkeby": "npx hardhat verify --network rinkeby"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "@nomiclabs/hardhat-etherscan": "^2.1.6",
    "@nomiclabs/hardhat-ganache": "^2.0.1",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "@types/chai": "^4.2.22",
    "@types/mocha": "^9.0.0",
    "@types/node": "^16.10.1",
    "chai": "^4.3.4",
    "ethereum-waffle": "^3.4.0",
    "ethers": "^5.4.7",
    "hardhat": "^2.6.4",
    "hardhat-dependency-compiler": "^1.1.1",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.2.2",
    "dotenv": "^10.0.0"
  }
}
